---
// Component for a floating WhatsApp button that appears on all pages
---

<div class="whatsapp-container fixed z-[100] flex items-center">
  <a 
    href="https://wa.me/40720123144" 
    target="_blank" 
    rel="noopener noreferrer"
    class="whatsapp-button flex items-center bg-[#25D366] text-white rounded-md shadow-lg transition-all duration-300 hover:pr-6 overflow-hidden group"
    aria-label="Contact pe WhatsApp"
  >
    <!-- WhatsApp Icon with pulse effect -->
    <div class="flex items-center justify-center p-3 relative">
      <div class="absolute inset-0 bg-white/20 rounded-md opacity-0 pulse-animation"></div>
      <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="w-6 h-6">
        <path d="M3 21l1.65-3.8a9 9 0 1 1 3.4 2.9L3 21"></path>
        <path d="M9 10a.5.5 0 0 0 1 0V9a.5.5 0 0 0-1 0v1Z"></path>
        <path d="M13.5 10a.5.5 0 0 0 1 0V9a.5.5 0 0 0-1 0v1Z"></path>
        <path d="M9 13c0 .83.67 1.5 1.5 1.5h3c.83 0 1.5-.67 1.5-1.5v-.5H9v.5Z"></path>
      </svg>
    </div>
    
    <!-- Text (hidden by default, shown on hover) -->
    <span class="font-medium text-sm whitespace-nowrap max-w-0 group-hover:max-w-[120px] transition-all duration-300 overflow-hidden opacity-0 group-hover:opacity-100 pr-0 group-hover:pr-2">
      Contactează-ne
    </span>
  </a>
</div>

<style>
  .whatsapp-container {
    right: 1rem;
    bottom: 6rem;
  }

  @media (max-width: 640px) {
    .whatsapp-container {
      bottom: 5rem;
      right: 1rem;
    }
  }

  .whatsapp-button {
    transform: translateZ(0); /* Force hardware acceleration */
    will-change: width; /* Optimize for animation */
    transition: all 0.3s ease-in-out;
  }
  
  .whatsapp-button:hover {
    box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
  }
  
  @keyframes pulse {
    0% {
      transform: scale(0.95);
      opacity: 0.5;
    }
    70% {
      transform: scale(1.1);
      opacity: 0.1;
    }
    100% {
      transform: scale(0.95);
      opacity: 0;
    }
  }
  
  .pulse-animation {
    animation: pulse 2s infinite;
  }
</style> 